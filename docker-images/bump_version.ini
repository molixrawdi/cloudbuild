[bumpversion]
current_version = 0.1.0
commit = True
tag = True
tag_name = v{new_version}
message = Bump version: {current_version} → {new_version}

[bumpversion:file:VERSION]

[bumpversion:file:Dockerfile]
search = LABEL version="{current_version}"
replace = LABEL version="{new_version}"

[bumpversion:file:docker-compose.yml]
search = image: myapp:{current_version}
replace = image: myapp:{new_version}
# pip install bump2version

                        subprocess.run(['gcloud', 'run', 'deploy', 'my-app',
                                        '--image', os.environ['IMAGE_TAG'],
                                        '--platform', 'managed',
                                        '--region', 'us-central1'], check=True)
          EOF
          
